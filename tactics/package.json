{
  "name": "tactics",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cp index.src.html index.html && vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cp index.src.html index.html && vite build && rm -rf assets && cp -r dist/assets assets && cp dist/index.html index.html && cp public/*.png . && echo 'Deployed!'"
  },
  "dependencies": {
    "konva": "^10.2.3",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-konva": "^19.2.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.1"
  }
}
